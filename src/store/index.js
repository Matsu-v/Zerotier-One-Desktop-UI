import { createStore } from 'vuex'

export default createStore({
  state: {
    Token:process.env.VUE_APP_ZT_AUTH,
    darkMode: true,
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  }
})
